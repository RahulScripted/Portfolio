# Web3Forms access key used by the contact and book-call forms.
# This is a public (submit-only) key, safe to expose in the frontend bundle.
# Copy this file to `.env` and set your own key to override the built-in default.
VITE_WEB3FORMS_KEY=
